# Licenses and Attribution

## Application Code

- Repository code: Apache-2.0 (`LICENSE`).

## Runtime Libraries

- React, Vite ecosystem, Luxon, Zustand, D3-geo, Three, R3F, SunCalc, topojson-client: permissive OSS licenses (MIT/BSD style).

## Data

- World basemap topology is sourced from `world-atlas` (derived from Natural Earth, public domain).
- `public/data/landmarks_core.json` and `public/data/cities_ne_min.json` are curated project datasets.

## Optional Datasets

- If timezone polygon boundaries are added in `data/timezones`, keep ODbL attribution and notices in that folder.
- Runtime file for accuracy mode should be placed at `public/data/timezones/timezones.geojson`.
