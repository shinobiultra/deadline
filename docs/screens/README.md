# docs/screens

Deterministic UI screenshots generated by:

```bash
npm run ui:capture
```

The capture pipeline uses demo mode (`?demo=1`) with a fixed deadline/time baseline so screenshots stay stable across runs.

Do not edit screenshots manually.
